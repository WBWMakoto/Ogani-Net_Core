@page
@model PersonalDataModel
@{
    ViewData["Title"] = "Dữ liệu cá nhân";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card p-4 shadow-lg" style="max-width: 500px; width: 100%; border-radius: 10px;">
        <h2 class="text-center fw-bold"><i class="fas fa-user-secret"></i> @ViewData["Title"]</h2>

        <div class="mt-3">
            <p>
                Tài khoản của bạn chứa dữ liệu cá nhân mà bạn đã cung cấp. Trang này cho phép bạn tải xuống hoặc xóa dữ liệu đó.
            </p>
            <p class="text-danger fw-bold">
                <i class="fas fa-exclamation-triangle"></i> Việc xóa dữ liệu này sẽ <u>vĩnh viễn xóa tài khoản của bạn</u> và không thể khôi phục lại.
            </p>
        </div>

        <div class="d-flex flex-column gap-2 mt-3">
            <form id="download-data" asp-page="DownloadPersonalData" method="post">
                <button class="btn btn-primary w-100">
                    <i class="fas fa-download"></i> Tải xuống dữ liệu
                </button>
            </form>

            <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger w-100">
                <i class="fas fa-trash-alt"></i> Xóa tài khoản
            </a>
        </div>

        <div class="text-center mt-3">
            <a asp-page="./Index" class="btn btn-link">
                <i class="fas fa-arrow-left"></i> Quay lại hồ sơ
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
