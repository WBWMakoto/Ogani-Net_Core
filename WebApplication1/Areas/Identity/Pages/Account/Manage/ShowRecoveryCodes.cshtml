@page
@model ShowRecoveryCodesModel
@{
    ViewData["Title"] = "Mã khôi phục";
    ViewData["ActivePage"] = "TwoFactorAuthentication";
}

<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card p-4 shadow-lg" style="max-width: 600px; width: 100%; border-radius: 10px;">
        <h2 class="text-center fw-bold"><i class="fas fa-key"></i> @ViewData["Title"]</h2>

        <partial name="_StatusMessage" model="Model.StatusMessage" />

        <div class="alert alert-warning mt-3" role="alert">
            <p><strong>Hãy lưu giữ các mã này ở nơi an toàn!</strong></p>
            <p>Nếu bạn mất thiết bị và không có mã khôi phục, bạn sẽ không thể truy cập vào tài khoản của mình.</p>
        </div>

        <div class="text-center mt-3">
            <h5><i class="fas fa-lock"></i> Danh sách mã khôi phục</h5>
            <div class="mt-2 p-3 bg-light rounded text-center">
                @for (var row = 0; row < Model.RecoveryCodes.Length; row += 2)
                {
                    <code class="d-block fs-5 text-primary fw-bold">@Model.RecoveryCodes[row]</code>
                    <code class="d-block fs-5 text-primary fw-bold">@Model.RecoveryCodes[row + 1]</code>
                    <hr class="my-2" />
                }
            </div>
        </div>

        <div class="d-grid gap-2 mt-4">
            <a asp-page="./GenerateRecoveryCodes" class="btn btn-warning">
                <i class="fas fa-sync-alt"></i> Tạo lại mã khôi phục
            </a>
            <a asp-page="./TwoFactorAuthentication" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Quay lại cài đặt 2FA
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
