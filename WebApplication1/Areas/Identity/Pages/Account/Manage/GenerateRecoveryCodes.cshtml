@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Tạo mã khôi phục xác thực hai yếu tố (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card p-4 shadow-lg" style="max-width: 600px; width: 100%; border-radius: 10px;">
        <h2 class="text-center fw-bold text-primary">
            <i class="fas fa-key"></i> @ViewData["Title"]
        </h2>

        <div class="alert alert-warning mt-3">
            <p class="fw-bold">
                <i class="fas fa-exclamation-triangle"></i> Vui lòng lưu lại các mã này ở nơi an toàn!
            </p>
            <p>
                Nếu bạn mất thiết bị và không có mã khôi phục, bạn sẽ không thể truy cập tài khoản của mình.
            </p>
            <p>
                Việc tạo mã khôi phục mới <strong>không thay đổi khóa xác thực</strong> trong ứng dụng bảo mật.
                Nếu bạn muốn thay đổi khóa, hãy <a asp-page="./ResetAuthenticator">đặt lại khóa xác thực</a>.
            </p>
        </div>

        <div class="d-flex flex-column gap-2 mt-3">
            <form method="post">
                <button class="btn btn-danger w-100">
                    <i class="fas fa-redo-alt"></i> Tạo mã khôi phục mới
                </button>
            </form>
        </div>

        <div class="text-center mt-3">
            <a asp-page="./TwoFactorAuthentication" class="btn btn-link">
                <i class="fas fa-arrow-left"></i> Quay lại xác thực 2FA
            </a>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
