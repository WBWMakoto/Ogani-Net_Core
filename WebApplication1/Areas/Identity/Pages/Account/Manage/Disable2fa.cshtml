@page
@model Disable2faModel
@{
    ViewData["Title"] = "Tắt xác thực hai yếu tố (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<div class="container d-flex justify-content-center align-items-center min-vh-100">
    <div class="card p-4 shadow-lg" style="max-width: 500px; width: 100%; border-radius: 10px;">
        <h2 class="text-center text-danger"><i class="fas fa-exclamation-triangle"></i> @ViewData["Title"]</h2>

        <partial name="_StatusMessage" for="StatusMessage" />

        <div class="alert alert-warning text-center" role="alert">
            <p><strong>Thao tác này chỉ vô hiệu hóa 2FA.</strong></p>
            <p>
                Tắt 2FA không thay đổi khóa đã được sử dụng trong ứng dụng xác thực. Nếu bạn muốn thay đổi khóa, hãy
                <a asp-page="./ResetAuthenticator" class="text-decoration-none fw-bold">đặt lại khóa xác thực</a>.
            </p>
        </div>

        <div class="text-center">
            <form method="post" class="form-group">
                <button class="btn btn-danger w-100 fw-bold" type="submit">
                    <i class="fas fa-ban"></i> Tắt Xác Thực Hai Yếu Tố
                </button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
